# Sentinel Monitor dashboard

This directory contains the React dashboard for Sentinel Monitor.

## Commands

```powershell
npm install
npm run dev
npm run build
```

The dashboard currently uses demonstrative in-memory data. It will connect to the FastAPI service as the backend endpoints are developed.
